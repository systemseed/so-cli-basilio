name: docker:shell
description: "Log into a specified container as a default user. Example: `make sh php`."
aliases:
  - sh
arguments:
  required: 1
command: |
  if [ -n "$so_arg_1" ]; then \
    docker compose exec $so_arg_1 sh; \
  else \
    docker compose exec php sh; \
  fi
